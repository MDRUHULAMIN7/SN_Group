# Public canonical origin used by metadata, sitemap, robots and JSON-LD.
NEXT_PUBLIC_SITE_URL=https://www.sngroup.com

# Optional server-side delivery adapter. Accepts a JSON POST payload.
# Leave unset until the client supplies an approved endpoint.
INQUIRY_WEBHOOK_URL=
